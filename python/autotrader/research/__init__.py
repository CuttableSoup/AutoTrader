"""Research-only code for the v2 hypothesis pre-tests (docs/prereg/).

Nothing on the live path imports this package. It reads vendor data from data/raw/
directly so that universe membership is decided point-in-time, without the
"ever above the floor" selection that data/sharadar/ carries.
"""
